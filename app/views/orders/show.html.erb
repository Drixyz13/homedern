<section class="py-5 mt-5 text-white" style="background-color: #0f0f0f;">
  <div class="container text-center">
    <h1 class="text-purple fw-bold mb-4">🎉 Merci pour votre commande !</h1>
    <p class="lead mb-4">Votre commande a bien été enregistrée. Un email de confirmation vous a été envoyé.</p>

    <div class="row justify-content-center">
      <div class="col-lg-8">
        <div class="card bg-dark text-white p-4 mb-4 shadow-lg text-start">
          <h5 class="fw-bold mb-4">🧾 Récapitulatif :</h5>

          <% @order.order_items.each do |item| %>
            <div class="d-flex align-items-center mb-3">
              <%= image_tag item.product.image_url, class: "rounded me-3", style: "width: 60px; height: 60px; object-fit: cover;" %>
              <div class="flex-grow-1">
                <p class="mb-0 fw-bold text-white"><%= item.product.name %></p>
                <p class="mb-0 text-white-50"><%= item.quantity %> × <%= number_to_currency(item.price_at_order / 100.0, unit: "€", format: "%n %u") %></p>
              </div>
            </div>
          <% end %>

          <hr class="my-4 text-white">

          <div class="d-flex justify-content-between align-items-center">
            <p class="fw-bold mb-0 text-white fs-5">💰 Total payé :</p>
            <p class="fw-bold mb-0 text-purple fs-5"><%= number_to_currency(@order.total_cents / 100.0, unit: "€", format: "%n %u") %></p>
          </div>

          <div class="mt-4">
            <p class="mb-1 text-white-50">
              📦 Livraison estimée : <%= l(@order.created_at + 5.days, format: :short) %>
            </p>
            <p class="text-white-50 mb-0 fst-italic">
              Comme indiqué dans le mail, vous allez recevoir un message de notre service de livraison pour planifier la réception de votre commande.
            </p>
          </div>
        </div>

        <%= link_to "← Retour à la boutique", products_path, class: "btn btn-outline-light w-100" %>
      </div>
    </div>
  </div>
</section>
